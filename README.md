# 🚀 Performance Review System

Современная система для проведения performance review в компании. Включает самооценку, оценку руководителей и оценку респондентов с автоматическим расчетом баллов и генерацией рекомендаций.

## 🛠 Технологии

| Компонент | Технология |
|-----------|------------|
| **Бэкенд** | FastAPI (Python 3.11+) |
| **Фронтенд** | ASP.NET Core 8.0 |
| **База данных** | PostgreSQL 16 |
| **Контейнеризация** | Docker & Docker Compose (в процессе) |
| **Документация** | Swagger/ReDoc |

## 📋 Предварительные требования

### Для локальной разработки:
- 🐍 Python 3.11+
- 🐘 PostgreSQL 16+
- ⚡ .NET 8.0 SDK
- 🔧 Git

---

## ⚡ Быстрый старт

### 🎯 Настройка бэкенда (FastAPI)

#### 1. Создайте виртуальное окружение

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

2. Установите зависимости 

```bash
pip install -r requirements.txt
```

3. Настройте базу данных
✅ Убедитесь, что PostgreSQL запущен

✅ Создайте базу данных performance_review

✅ Настройте пользователя review_user с паролем review_password

4. Настройте переменные окружения
```bash
cp .env.example .env
```
# Отредактируйте .env файл при необходимости

5. Инициализируйте базу данных
Очистка и создание таблиц:

```bash
python temporary_reset.py
```
Заполнение тестовыми данными:

```bash
python create_test_data.py
```
Импорт шаблонов вопросов:

```bash
python init_default_questions.py
```
6. Запустите сервер

```bash
uvicorn app.main:app --reload
```

🎨 Запуск фронтенда (ASP.NET Core)
Для корректной работы должны быть установлены библиотеки для работы C#

1. Перейдите в папку Front
```bash
cd Front
```

2. Выполните команды в терминале:
Настройка сертификата:

```bash
dotnet dev-certs https --trust
```

Запуск приложения:

```bash
dotnet run https
```

📚 Документация API
После запуска бэкенда документация доступна по адресам:

Документация	Ссылка
Swagger UI	http://localhost:8000/docs
ReDoc	http://localhost:8000/redoc

🧪 Тестирование
Для запуска тестов выполните команду:

```bash
pytest --cov=app tests/
```

🎯 Доступ к приложениям
После успешного запуска:

Приложение	URL

Бэкенд API	http://localhost:8000
Фронтенд	https://localhost:5001

Документация	http://localhost:8000/docs
🔧 Полезные команды
Бэкенд

```bash
# Запуск с перезагрузкой
uvicorn app.main:app --reload
```

# Запуск на конкретном хосте и порту
bash```uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload```

Фронтенд
```bash
# Восстановление зависимостей .NET
dotnet restore
```

# Сборка проекта
```bash dotnet build```

# Запуск с разными профилями
dotnet run --launch-profile https
